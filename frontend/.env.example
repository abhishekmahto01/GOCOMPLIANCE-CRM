# Gocompliances CRM Frontend Environment Configuration
# Leave empty for same-origin relative API proxying (demo mode / local Vite proxy / single-domain production):
# VITE_API_BASE_URL=
# Or specify an explicit backend origin for local development or multi-domain deployment:
VITE_API_BASE_URL=http://localhost:8000
